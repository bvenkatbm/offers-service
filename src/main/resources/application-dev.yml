spring:
  datasource:
    url: jdbc:h2:mem:offers
    driverClassName: org.h2.Driver
    username: sa
    password: password
    database-platform: org.hibernate.dialect.H2Dialect
  h2:
    console:
      enabled: true
      path: /h2-console

server:
  port: 8081

photos:
  base:
    url: https://jsonplaceholder.typicode.com/photos